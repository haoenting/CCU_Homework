#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define SIZE 1005

int main() {
    char buf[100];
    printf("Please enter a number (to quit, enter 'Quit', 'quit', or '-1'): ");

    scanf("%s", buf);
       

    return 0;
}
